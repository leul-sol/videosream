class AppConstants {
  static const String baseUrl = 'https://stream.mux.com';
  static const int maxCacheSize = 500 * 1024 * 1024;
  static const Duration maxVideoDuration = Duration(minutes: 5);
  static const Duration controlsAutoHideTime = Duration(seconds: 3);
}
